package model;

/**  This code is defining an enumeration type called `CategotyType` with three possible values:
 `VARIETY`, `DESIGN`, and `SCIENTIST`. This enum can be used to represent a set of related constants
 in a type-safe way.*/
public enum CategotyType {
    
    VARIETY, DESIGN, SCIENTIST;
}
