package model;

/**This code is defining an enumeration called `AdType` with three possible values: `COMBO_PLUS`,
`LAIKA`, and `EXITO`. An enumeration is a special type in Java that represents a fixed set of
constants. In this case, `AdType` is used to represent different types of advertisements.*/
public enum AdType {
    
    COMBO_PLUS, LAIKA, EXITO;

}
