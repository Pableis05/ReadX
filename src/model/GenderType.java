package model;

/** This code is defining an enumeration called `GenderType` with three possible values:
`SCIENCIE_FICTION`, `FANTASY`, and `HISTORICAL_NOVEL`. It is likely that this enumeration is being
used to represent different genres of literature or media.*/
public enum GenderType {
    SCIENCIE_FICTION, FANTASY, HISTORICAL_NOVEL;
}
